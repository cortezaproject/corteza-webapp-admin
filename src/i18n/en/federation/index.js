import nodes from './nodes'
import permissions from './permissions'

export default {
  federation: {
    navGroup: {
      label: 'Federation',
    },
  },

  'federation.nodes': nodes,
  'federation.permissions': permissions,
}
